# Copy this file's contents into Railway's "Variables" tab (or a local .env file if testing on your own machine).
# Do not commit a real .env file with actual keys to any public GitHub repo.

# Your Claude API key, from console.anthropic.com -> API Keys
ANTHROPIC_API_KEY=your-key-here

# Optional - Railway sets this automatically, only needed for local testing
PORT=3000
